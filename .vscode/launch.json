{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug con Chrome (localhost)",
            "url": "http://localhost:5173",
            "webRoot": "${workspaceFolder}",
            // Questa opzione aiuta a mappare correttamente i file svelte
            "sourceMaps": true
        }
    ]
}